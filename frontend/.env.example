# Frontend environment example for Vite (copy to .env and fill real values)
# Use the VITE_ prefix for variables to be exposed to the client.
# Base URL for Backend API and WebSocket
VITE_BASE_URL=https://api.your-domain.com
# Google Maps API key for client-side maps
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
