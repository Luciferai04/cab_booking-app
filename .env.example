# Root environment example for docker-compose and services
# Copy to .env and fill appropriate secure values before production

# Shared secrets
JWT_SECRET=change-me
DB_CONNECT=mongodb://mongo:27017/uber-video?directConnection=true
CORS_ORIGIN=http://localhost:5173,http://localhost:8080,https://localhost:8443

# External API keys (do not commit real keys)
GOOGLE_MAPS_API=dummy-key
WANDB_API_KEY=

