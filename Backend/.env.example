# Backend environment example (copy to .env and fill real values before production)
PORT=3000
DB_CONNECT=mongodb://127.0.0.1:27017/uber-video?directConnection=true
JWT_SECRET=change-me
GOOGLE_MAPS_API=your-google-maps-api-key
# Comma-separated list of allowed origins for CORS (use * for all, or set your frontend domain)
CORS_ORIGIN=*

